const questions = [
    {
        question: "Quel est l'outil de versionnement le plus utilisé ?",
        options: ["Git", "SVN", "Mercurial"],
        answer: 0
    }
    // Ajoute tes 1400 questions ici plus tard !
];

document.getElementById('start-btn').addEventListener('click', () => {
    startQuiz();
});

function startQuiz() {
    // Logique du quiz à venir
    alert("Quiz démarré !");
}